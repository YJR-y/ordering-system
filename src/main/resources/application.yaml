spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/ordering_system?useSSL=false&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root

  jpa:
    show-sql: true

server:
  servlet:
    context-path: /ordering_system
  port: 8080
  context-path: /sell

logging:
  pattern:
    console: "%d - %msg%n" # 缩短日志内容
